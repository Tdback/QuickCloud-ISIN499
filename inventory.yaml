---
quickcloud:
  hosts:
    jumpbox:
      ansible_port: "{{ ssh_port | default('22') }}"
      ansible_ssh_private_key_file: "~/.ssh/aws_ssh_testing"
      ansible_user: "admin"
      ansible_host: "{{ variable_host }}"

