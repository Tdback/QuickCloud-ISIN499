---
quickcloud:
  hosts:
    jumpbox:
      ansible_port: "{{ ssh_port }}"
      ansible_ssh_private_key_file: "{{ ssh_key_file }}"
      ansible_user: "{{ username }}"
      ansible_host: "{{ variable_host }}"

